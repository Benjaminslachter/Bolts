Programmer: Benjamin Slachter
Date: 5.3.2022
Program: Javascript

async function startProgram() {

setMainLed({ r: 0, g: 0, b: 255 });

await speak("Figure 8", true);

await delay(1);

setSpeed(75)

setMainLed(getRandomColor());

await spin(360, 4)

await spin(-360, 4)

stopRoll()

}
